# 证件照制作工具 · 使用说明（Windows）

## 一、启动

双击 `一键安装并运行.bat`（依赖已在本机装好，现在双击是秒开；换电脑才需要重新安装）

> 前置条件：已安装 Python 3.9~3.12，安装时勾选了 "Add Python to PATH"。

## 二、操作流程

1. **① 上传照片** → 点 `✂️ 一键抠图`（建议勾选"发丝边缘优化"）
2. **② 选尺寸**（一寸 / 二寸…）、**选底色**（红 / 白 / 蓝），用滑块微调人像大小与位置，右侧实时预览
3. **③ 选相纸**（5寸 / 6寸 / A4）和排版方式，右侧实时预览
4. **④ 点生成并导出**，得到 4 个文件：

| 文件 | 说明 |
| --- | --- |
| `证件照_xxx.png` | 单张，PNG 无损 |
| `证件照_xxx.jpg` | 单张，JPEG 100% 质量 |
| `排版_xxx.png` | 相纸排版，PNG 无损 |
| `排版_xxx.jpg` | 相纸排版，JPEG 100% 质量 |

全部 300 DPI、无压缩，打印店可直接使用。
输出目录：`C:\Users\<用户名>\AppData\Local\Temp\idphoto_out\`

## 三、输出规格（300 DPI）

| 规格 | 尺寸 (mm) | 像素 |
| --- | --- | --- |
| 一寸 | 25×35 | 295×413 |
| 小一寸 | 22×32 | 260×378 |
| 大一寸 | 33×48 | 390×567 |
| 二寸 | 35×49 | 413×579 |
| 小二寸 | 35×45 | 413×531 |

底色 RGB：红 `(255,0,0)` / 白 `(255,255,255)` / 蓝 `(67,142,219)`

## 四、离线 / 分发

- ✅ 抠图模型 `u2net_human_seg.onnx`（168MB）**已放在 `models/` 目录**，本机可完全离线抠图，不会再去联网下载
- 想发给别人免安装使用：双击 `打包成exe.bat`，产出 `dist\证件照工具\`，整个文件夹压缩发送

## 五、打包成免安装 exe

> ✅ **已经打好**：`dist\证件照工具\证件照工具.exe`（28MB）+ `_internal\`（578MB，含模型和依赖），整个文件夹 605MB。
> 对方拿到这个文件夹后**双击 `证件照工具.exe` 就能用**，不需要装 Python、也不需要联网下模型。

如需重新打包：双击 `打包成exe.bat`（首次会先装 PyInstaller，然后约 10 分钟出包）。

## 六、常见问题

- **抠图边缘不够干净**：勾选"发丝边缘优化"；或把 `idphoto.py` 里 `new_session("u2net_human_seg")` 改成 `new_session("isnet-general-use")` 或 `new_session("birefnet-portrait")`（更精细，首次需重新下载模型）
- **导出的图模糊**：取决于原图，请用手机原图（宽 ≥ 1000 像素），不要用微信压缩过的图
- **提示"放不下"**：换 A4 相纸，或减少排版张数
- **依赖装不上**：确认 Python 版本 3.9~3.12；删掉 `venv` 文件夹重新运行脚本
- **端口 7860 被占用**：把 `idphoto.py` 末尾改成 `demo.launch(inbrowser=True, server_port=7861)`
